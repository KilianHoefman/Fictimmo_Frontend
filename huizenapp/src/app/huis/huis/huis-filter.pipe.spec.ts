import { HuisFilterPipe } from './huis-filter.pipe';

describe('HuisFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new HuisFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
